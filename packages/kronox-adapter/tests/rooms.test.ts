import { describe, expect, test } from '@jest/globals';
import { getRoomBookings } from '../src/rooms';
describe('Rooms Module', () => {
    test('Test', () => {
        expect(true).toBe(true);
    });
});